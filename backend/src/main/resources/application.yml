spring:
  application:
    name: transitid
  datasource:
    url: jdbc:postgresql://localhost:5432/transitid
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: super-secret-transitid-key-make-it-long-enough-for-hs256
  expiration: 86400000 # 1 day

management:
  endpoints:
    web:
      exposure:
        include: health,info
